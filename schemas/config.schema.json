{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"type": "object",
	"properties": {
		"dartBarrel.files.disableRecursiveBarrelling": {
			"type": "boolean",
			"default": false,
			"scope": "resource",
			"description": "Disable the recurtion for subdirectories when creating the barrel file."
		},
		"dartBarrel.files.includeExtensionOnExport": {
			"type": "array",
			"default": ["dart"],
			"scope": "resource",
			"description": "Extensions to include on the export. This is used to filter the files to export."
		},
		"dartBarrel.files.ignoreFilePathPatternOnExport": {
			"type": "array",
			"default": ["**/*.freezed.dart", "**/*.g.dart"],
			"scope": "resource",
			"description": "Glob patterns to ignore on the export. This is used to filter the files to export."
		},
		"dartBarrel.files.supportsHiddenFiles": {
			"type": "boolean",
			"default": true,
			"scope": "resource",
			"description": "Support hidden files and directories when exporting (files starting with a dot)."
		},
		"dartBarrel.files.preserveGitignoreSettings": {
			"type": "boolean",
			"default": false,
			"scope": "resource",
			"description": "Preserve the settings from the .gitignore file when exporting."
		},
		"dartBarrel.files.enableClassExportDetection": {
			"type": "boolean",
			"default": true,
			"scope": "resource",
			"description": "Enable class export detection."
		},
		"dartBarrel.files.exportDefaultFilename": {
			"type": "string",
			"default": "",
			"scope": "resource",
			"description": "Default filename to use when exporting the files."
		},
		"dartBarrel.files.outputFilePrefix": {
			"type": "string",
			"default": "",
			"scope": "resource",
			"description": "Prefix to use for the output file."
		},
		"dartBarrel.files.outputFileSuffix": {
			"type": "string",
			"default": "",
			"scope": "resource",
			"description": "Suffix to use for the output file."
		},
		"dartBarrel.files.outputFileSeparator": {
			"type": "string",
			"default": "_",
			"scope": "resource",
			"description": "Separator to use for the output file."
		},
		"dartBarrel.formatting.endOfLine": {
			"type": "string",
			"default": "lf",
			"enum": ["crlf", "lf"],
			"enumDescriptions": ["CRLF", "LF"],
			"scope": "resource",
			"description": "End of line character to use."
		},
		"dartBarrel.formatting.insertFinalNewline": {
			"type": "boolean",
			"default": true,
			"scope": "resource",
			"description": "Insert a final newline at the end of the file."
		}
	}
}
